


// used in heavy string parsing and injecting operations 
class orderedOperations {
    constructor (enumOpTypes) {
        /**enumOpTypes
         * Used by custom replacement algorithm
         */
        this._operationsArray = []
    }

    /**Add a operation to the operation Index 
     * 
     * @param {Number} startingIndex 
     * @param {Number} endIndex 
     * @param  {...any} customData 
     */
    add(startingIndex, endIndex, ...customData) {

    }

}